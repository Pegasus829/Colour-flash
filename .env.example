# AWS Configuration for Centralized Leaderboard
#
# Setup Instructions:
# 1. Create a Cognito Identity Pool (with unauthenticated access enabled)
# 2. Create a DynamoDB table with:
#    - Partition key: pk (String)
#    - Sort key: sk (String)
# 3. Attach an IAM policy to the Cognito unauthenticated role allowing:
#    - dynamodb:PutItem
#    - dynamodb:Scan
#    on your DynamoDB table

VITE_AWS_REGION=us-east-1
VITE_AWS_IDENTITY_POOL_ID=us-east-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
VITE_AWS_DYNAMODB_TABLE=color-match-rush-scores
